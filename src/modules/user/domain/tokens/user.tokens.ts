/**
 * Injection tokens — plain symbols so the domain layer stays framework-free.
 */
export const USER_REPOSITORY = Symbol('USER_REPOSITORY');
export const PASSWORD_HASHER = Symbol('PASSWORD_HASHER');
